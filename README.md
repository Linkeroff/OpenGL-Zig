# OpenGL-Zig

A small project with bindings for OpenGL-related technologies

Files:
- `gl.zig` - the binding code
- `OpenGL ES 1.1 List.txt` - list of IDs

The project may contain errors or omissions
